<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:DailyProject_221204"
    >

    <Style
        x:Key="TaskListItemViewDefaultStyle"
        TargetType="{x:Type local:TaskListItemView}"
        >

        <Setter Property="Template">

            <Setter.Value>

                <ControlTemplate>

                    <Border
                        x:Name="_taskBorder"
                        Height="48"
                        BorderBrush="White"
                        BorderThickness="1"
                        CornerRadius="2"
                        DockPanel.Dock="Top"
                        >

                        <Grid Margin="2">

                            <Grid.RowDefinitions>

                                <RowDefinition Height="1*" />

                            </Grid.RowDefinitions>

                            <Grid
                                Grid.Row="0"
                                Margin="4,4,4,2"
                                >

                                <Grid.ColumnDefinitions>

                                    <ColumnDefinition Width="3*" />
                                    <ColumnDefinition Width="1*" />

                                </Grid.ColumnDefinitions>

                                <Button
                                    x:Name="_taskTitleButton"
                                    Grid.Column="0"
                                    Padding="4"
                                    HorizontalContentAlignment="Left"
                                    Background="#FF3F3F3F"
                                    BorderBrush="#FF252525"
                                    Foreground="White"
                                    >

                                </Button>

                                <Button
                                    x:Name="_taskStatusButton"
                                    Grid.Column="1"
                                    Padding="4"
                                    HorizontalContentAlignment="Left"
                                    Background="#FF3F3F3F"
                                    BorderBrush="#FF252525"
                                    Foreground="White"
                                    >

                                </Button>

                            </Grid>
                            <!--
                            <Grid Grid.Row="1" Margin="4,2,4,4" >

                                <Grid.ColumnDefinitions>

                                    <ColumnDefinition Width="32"/>
                                    <ColumnDefinition Width="*"/>

                                </Grid.ColumnDefinitions>

                                <Image Grid.Column="0" HorizontalAlignment="Center" VerticalAlignment="Center">

                                </Image>

                                <Button Grid.Column="1" x:Name="_taskStatusButton" Padding="4" Foreground="White" Background="#FF3F3F3F" BorderBrush="#FF252525" HorizontalContentAlignment="Left">

                                </Button>

                            </Grid>-->
                        </Grid>

                    </Border>

                </ControlTemplate>

            </Setter.Value>

        </Setter>

    </Style>

</ResourceDictionary>